<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="isl.ayaz">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_isllogo_round"
        android:label="@string/app_name"
        android:roundIcon="@drawable/isllogo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.Splash"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.HomeActivity" />
        <activity android:name=".activities.BlogActivity" />
        <activity android:name=".activities.ExamActivity" />
        <activity android:name=".syllabus.CSEActivity" />
        <activity android:name=".syllabus.ECEActivity" />
        <activity android:name=".syllabus.EEEActivity" />
        <activity android:name=".syllabus.ITActivity" />
        <activity android:name=".syllabus.MECHActivity" />
        <activity android:name=".syllabus.CIVILActivity" />
        <activity android:name=".activities.BBlogActivity" />
        <activity android:name=".activities.SNPActivity" />
        <activity android:name=".papers.CSEPapers" />
        <activity android:name=".papers.ECEPapers" />
        <activity android:name=".papers.CIVILPapers" />
        <activity android:name=".papers.MECHPapers" />
        <activity android:name=".papers.EEEPapers" />
        <activity android:name=".activities.Team" />
        <activity android:name=".papers.ITPapers" />
        <activity
            android:name=".activities.AboutActivity"
            android:label="@string/title_activity_about"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activities.NewsActivity" />
        <activity android:name=".activities.EventActivity" />
        <activity android:name=".DeptActivity" />
        <activity
            android:name=".dept.CSEDept"
            android:label="@string/title_activity_csedept"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".dept.CEDept"
            android:label="@string/title_activity_cedept"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".dept.ECEDept"
            android:label="@string/title_activity_ecedept"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".dept.EEEDept"
            android:label="@string/title_activity_eeedept"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".dept.MEDept"
            android:label="@string/title_activity_medept"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".USPActivity" />
        <activity
            android:name=".ScrollingActivity"
            android:label="@string/title_activity_scrolling"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Contact"
            android:label="@string/title_activity_contact"></activity>
    </application>

</manifest>